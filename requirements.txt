# Environment for the src/ streaming pipeline (Python 3.10, CPU-only).
# The conda environment.yml in MMS-FPI-Data-Gaps/ is the OLD PyTorch 1.4 / Python 3.7
# stack used by the original notebooks and does not match this code.
tensorflow==2.9.1
numpy==1.23.2
scipy==1.9.0
cdflib==1.3.10
matplotlib
requests
